{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <h1 class="mb-4 text-center">Edit {{ user.username }}</h1>

                <div class="card">
                    <div class="card-body">
                        {{ form_start(form) }}

                        <div class="form-group mb-3">
                            {{ form_label(form.roles, null, {'label_attr': {'class': 'form-label'}}) }}
                            {{ form_widget(form.roles, {'attr': {'class': 'form-control'}}) }}
                            <div class="text-danger">
                                {{ form_errors(form.roles) }}
                            </div>
                        </div>

                        <button class="btn btn-primary w-100" type="submit">Update User</button>
                        {{ form_end(form) }}
                    </div>
                </div>

                <div class="mt-3 text-center">
                    <a href="{{ path('list_users') }}" class="btn btn-secondary">Back to users</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
