{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <h1 class="mb-4">Create Editor</h1>
                {{ form_start(form) }}

                <div class="mb-3">
                    {{ form_label(form.name, null, {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.name) }}
                </div>

                <div class="mb-3">
                    {{ form_label(form.editorNumber, null, {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_widget(form.editorNumber, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.editorNumber) }}
                </div>

                <div class="mb-3">
                    {{ form_label(form.specialty, null, {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_widget(form.specialty, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.specialty) }}
                </div>

                <div class="mb-3">
                    {{ form_label(form.books, null, {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_widget(form.books, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.books) }}
                </div>

                <button type="submit" class="btn btn-success">Create</button>
                <a href="{{ path('editor_index') }}" class="btn btn-secondary">Back to List</a>

                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
